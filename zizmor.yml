# Every third-party action must be pinned to a full commit SHA. A tag is a
# mutable pointer some other account controls, so a tag-pinned action is a
# supply-chain hole: the code CI runs can change without a commit here.
rules:
  unpinned-uses:
    config:
      policies:
        "*": hash-pin
